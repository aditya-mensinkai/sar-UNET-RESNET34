# Direct dependencies for tools/*.py, preprocessing/*.py, and postprocessing/*.py
# (install: pip install -r requirements.txt)
# Verified against the working env on 2026-09-21: Python 3.14.7, RTX 3050,
# CUDA 12.6 — torch 2.14.0+cu126, torchvision 0.29.0+cpu,
# segmentation-models-pytorch 0.5.0, numpy 2.5.3, rasterio 1.5.1,
# matplotlib 3.11.2, pillow 12.3.0, tqdm 4.70.0.
# preprocessing/ is intentionally numpy+rasterio+matplotlib only: no scipy,
# torch, cv2, or albumentations, so the same 16 GB Windows box runs it.
# postprocessing/ uses numpy + rasterio + affine (stdlib struct for SHP; no
# scipy, geopandas, fiona, or osgeo required).
# The zero-value investigations (local + source-level) used this same set
# plus stdlib only; downloaded/source-analysis artifacts go to %TEMP%.
rasterio==1.5.1
affine==3.0.1      # installed as rasterio dependency; used directly by postprocessing/
numpy==2.5.3
matplotlib==3.11.2
pillow==12.3.0
tqdm==4.70.0
# Smoke-test / training only (NOT needed for preprocessing or audits).
# CUDA build for RTX 3050: pip install "torch==2.14.0+cu126" --index-url https://download.pytorch.org/whl/cu126
torch==2.14.0
torchvision==0.29.0
segmentation-models-pytorch==0.5.0
